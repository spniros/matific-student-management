import { MarksColorDirective } from './marks-color.directive';

describe('MarksColorDirective', () => {
  it('should create an instance', () => {
    // const directive = new MarksColorDirective();
    // expect(directive).toBeTruthy();
  });
});
